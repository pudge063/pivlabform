project_settings:
  description: "description for project from `single project.yaml`"
  squash_option: "default_on"
  variables:
    RELEASE_VERSION:
      key: RELEASE_VERSION
      value: "Q4_28"
      description:
      environment_scope: production
      filter:
      masked: false
      protected: false
      raw: false
      variable_type: env_var
    PYTHON_VERSION:
      key: PYTHON_VERSION
      value: "3.11.10"
      description: python version
      filter:
      masked: false
      protected: false
      raw: false
      variable_type: env_var
    SECRET_TOKEN:
      key: SECRET_TOKEN
      value: $TOKEN
      description: secret token
      filter:
      masked: false
      protected: false
      raw: true
      variable_type: env_var

projects:
  - 2307
